[project]
name = "sportslab-analytics"
version = "0.7.3-alpha"
description = "Open-source core for reproducible football tournament analytics."
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [
  {name = "SportsLabResearch"}
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
